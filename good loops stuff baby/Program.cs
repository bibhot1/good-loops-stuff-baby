// lets do for loop which counts up andprint hello world for 10 times
for (int i = 0; i <=10 ; i++)
{
    Console.WriteLine("Hello I" + i);
}

// now lets do wnother for loop which counts down from 10 to 1 instead
for (int i = 10; i >= 0; i--)
{
    Console.WriteLine("Hello I" + i);
}